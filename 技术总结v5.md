# FileVault v5.0 技术总结

**日期**: 2026-04-26
**版本**: 5.0.0

---

## 一、项目概述

FileVault 是一个基于 Node.js 的**局域网文件同步与分享系统**，支持多端访问（Windows PC、安卓手机等）。

### 核心功能
- 文件上传/下载/删除
- 文件夹管理（创建、删除、嵌套）
- 实时同步（WebSocket）
- 二维码分享
- 跨 WiFi 访问（Tailscale）
- 移动端适配

---

## 二、技术架构

### 2.1 目录结构

```
filevault/
├── src/
│   ├── server.js           # 主服务器入口
│   ├── config.js           # 配置文件
│   ├── websocket.js        # WebSocket 服务
│   ├── routes/
│   │   ├── files.js        # 文件 API
│   │   ├── folders.js      # 文件夹 API
│   │   └── share.js        # 分享 API
│   ├── services/
│   │   ├── dbService.js    # SQLite 数据库
│   │   ├── fileService.js  # 文件业务逻辑
│   │   └── folderService.js # 文件夹业务逻辑
│   └── middleware/
│       ├── security.js     # 安全中间件
│       └── cors.js        # CORS 配置
├── public/
│   ├── index.html         # 主页面
│   ├── share.html         # 分享下载页
│   └── js/
│       └── app.js         # 前端逻辑
├── database/               # SQLite 数据库文件
├── data/                   # 文件存储目录
├── package.json
└── server.js              # 启动脚本
```

### 2.2 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 后端 | Node.js | 事件驱动、非阻塞 I/O |
| 数据库 | sql.js | SQLite 的纯 JS 实现 |
| 实时通信 | ws | WebSocket |
| 前端 | Vanilla JS | 无框架依赖 |

### 2.3 数据库设计

```sql
-- 文件表
CREATE TABLE files (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    size INTEGER DEFAULT 0,
    type TEXT DEFAULT '',
    category TEXT DEFAULT 'other',
    folder_id TEXT,
    upload_time INTEGER,
    time_category TEXT DEFAULT 'older',
    created_at INTEGER
);

-- 文件夹表
CREATE TABLE folders (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    parent_id TEXT,
    created_at INTEGER,
    file_count INTEGER DEFAULT 0
);

-- 索引
CREATE INDEX idx_files_folder ON files(folder_id);
CREATE INDEX idx_files_name ON files(name);
CREATE INDEX idx_folders_parent ON folders(parent_id);
```

---

## 三、API 设计

### 3.1 文件 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/files | 获取文件列表 |
| POST | /api/files | 创建文件元数据 |
| GET | /api/files/:id | 获取文件信息 |
| PUT | /api/files/:id | 更新文件（重命名） |
| DELETE | /api/files/:id | 删除文件 |
| POST | /api/upload/:id | 上传文件内容 |
| GET | /api/download/:id | 下载文件 |
| GET | /api/search?q=keyword | 搜索文件 |
| PUT | /api/files/:id/move | 移动文件 |

### 3.2 文件夹 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/folders | 获取文件夹列表 |
| POST | /api/folders | 创建文件夹 |
| GET | /api/folders/:id | 获取文件夹信息 |
| PUT | /api/folders/:id | 更新文件夹 |
| DELETE | /api/folders/:id | 删除文件夹 |
| GET | /api/folders/:id/info | 获取文件夹内容 |
| GET | /api/folders/:id/download | 下载文件夹 |

### 3.3 其他 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/ip | 获取服务器 IP |
| GET | /api/port | 获取服务器端口 |

---

## 四、安全措施

### 4.1 文件 ID 验证
```javascript
function isValidFileId(fileId) {
    return /^[a-zA-Z0-9_\-\.]+$/.test(fileId);
}
```

### 4.2 路径安全
```javascript
function getSafeFilePath(fileId) {
    const filePath = path.join(CONFIG.DATA_DIR, fileId);
    const resolvedPath = path.resolve(filePath);
    const resolvedDataDir = path.resolve(CONFIG.DATA_DIR);
    return resolvedPath.startsWith(resolvedDataDir) ? resolvedPath : null;
}
```

### 4.3 HTML 转义
```javascript
function escapeHtml(text) {
    return text
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#039;');
}
```

---

## 五、文件分类

| 分类 | 扩展名 |
|------|--------|
| image | jpg, jpeg, png, gif, bmp, webp, svg, ico |
| document | pdf, doc, docx, xls, xlsx, ppt, pptx, txt, rtf |
| video | mp4, avi, mkv, mov, wmv, flv, webm |
| audio | mp3, wav, flac, aac, ogg, wma, m4a |
| other | 其他所有 |

---

## 六、前端功能

### 6.1 核心功能
- 网格/列表视图切换
- 文件筛选（按分类、时间）
- 搜索文件
- 拖拽上传
- 文件夹上传（保留目录结构）
- 批量选择和删除
- 右键菜单（重命名、移动、删除、下载、分享）
- 面包屑导航（嵌套文件夹）
- 图片预览

### 6.2 移动端适配
- 响应式布局
- 底部导航栏
- 触摸优化（长按菜单）
- 拍照上传

---

## 七、部署与使用

### 7.1 启动
```bash
npm install
npm start
```

### 7.2 访问地址
- 本机：`http://localhost:8888`
- 局域网：`http://<IP>:8888`

### 7.3 跨网络访问（Tailscale）

1. Windows 和安卓都安装 Tailscale
2. 使用同一账号登录
3. 设备自动出现在虚拟网络
4. 通过 Tailscale IP 访问

**Tailscale 下载：**
- Windows: https://tailscale.com/download/windows
- 安卓: https://tailscale.com/download/android

---

## 八、与旧版本对比

| 项目 | 旧版本 (v4) | 新版本 (v5) |
|------|-------------|-------------|
| 架构 | 单体文件 | 模块化分层 |
| 数据库 | JSON 文件 | SQLite |
| 文件上传 | 内存缓冲 | 流式处理 |
| 实时通信 | 轮询 | WebSocket |
| 分类 | 4 种 | 5 种（新增视频/音频） |
| 文件夹 | 扁平 | 支持嵌套 |
| 搜索 | 无 | 实时搜索 |
| 重命名 | 无 | 支持 |
| 移动 | 无 | 完善适配 |

---

## 九、后续优化建议

1. **大文件分片上传** - 支持断点续传
2. **用户认证** - 添加密码保护
3. **文件预览** - 预览更多类型（PDF、视频）
4. **文件夹压缩包** - 服务端直接生成 ZIP
5. **文件标签** - 支持多维度分类
6. **操作日志** - 审计追踪
